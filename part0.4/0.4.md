```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save

    browser->>server: POST /new_note (form data)
    server-->>browser: 302 Found (redirect to /notes)

    browser->>server: GET /notes
    server-->>browser: 200 OK (HTML document)

    browser->>server: GET /main.css
    server-->>browser: 200 OK (CSS)

    browser->>server: GET /main.js
    server-->>browser: 200 OK (JavaScript)

    browser->>server: GET /data.json
    server-->>browser: 200 OK (JSON notes)

    Note right of browser: Browser executes JS and renders the updated notes list